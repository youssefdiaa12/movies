import 'package:flutter/material.dart';

class browseTap extends StatelessWidget {
  const browseTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

